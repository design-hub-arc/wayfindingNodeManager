/**
 * This package handles file helpers, which tell how to read and write the imports
 * and exports of Wayfinding data. The VersionLog and WayfindingManifest were
 * used in the past to support Google Drive, but aren't being used as of now,
 * but we may need them in the future.
 */
package nodemanager.files;
