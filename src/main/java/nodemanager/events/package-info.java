/**
 * This package handles events, which are used to support undo and redo.
 * These events should be created, then logged in the NodeManager class
 * when the user alters the Graph data using the GUI.
 */
package nodemanager.events;
