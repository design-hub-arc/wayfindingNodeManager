package nodemanager;

/*
Implemented thus far:
NONE                                        - done
ADD               - in MapImage             - done
MOVE              - in MapImage             - done, but not super good. Add reset position option
REMOVE_CONNECTION - in NodeIcon             - not done
ADD_CONNECTION    - in NodeIcon             - not done
*/
public enum Mode {
    NONE,
    ADD,
    MOVE,
    REMOVE_CONNECTION,
    ADD_CONNECTION
}
